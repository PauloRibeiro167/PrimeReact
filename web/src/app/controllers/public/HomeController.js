import { someHelperFunction } from '@helpersPublic/Homehelpers';
import { HomeModel } from '@models/HomeModel';

export class HomeController {
  // Método que busca itens da página inicial
  fetchHomeItems() {
    return someHelperFunction();
  }

  // Método index que abre a página inicial
  index() {
  }
}